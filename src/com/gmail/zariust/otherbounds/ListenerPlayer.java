package com.gmail.zariust.otherbounds;

import org.bukkit.event.Listener;

// Do we even need a player listener?
// Perhaps we do if enabled in options - not sure which is best method yet (onPlayerMove or aSync task)

public class ListenerPlayer implements Listener {
    
}